import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.react_capacitor_custom_native_plugin',
  appName: 'react_capacitor_custom_native_plugin',
  webDir: 'build'
};

export default config;
